# Test Environment Configuration
# This file is used during pytest execution

# Test database (in-memory for speed)
DATABASE_URL=sqlite:///:memory:
TEST_DB_PATH=tests/fixtures/test_analyze_fin.db

# Test data paths
TEST_DATA_DIR=tests/fixtures
TEST_STATEMENTS_DIR=tests/fixtures/sample_statements
TEST_MERCHANT_MAPPING=tests/fixtures/test_merchant_mapping.json

# API settings (if needed)
API_URL=http://localhost:3001/api

# Feature flags for testing
ENABLE_STRICT_VALIDATION=true
DEBUG_MODE=true

# Test user credentials (if applicable)
TEST_USER_EMAIL=test@example.com
